---
const { imageUrl, title, text } = Astro.props;
---
<section>
    <h2>{title}</h2>
    <ul>
        <li>
            <img src={imageUrl} alt={title} />
            <p>{text}</p>
        </li>
    </ul>
</section>